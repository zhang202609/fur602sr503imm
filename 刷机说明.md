# ImmortalWrt 24.10 — 荣耀 FUR-602 刷机说明

## 固件信息

| 项目 | 内容 |
|---|---|
| 系统 | ImmortalWrt 24.10（社区 mt798x 分支） |
| 内核 | Linux 6.6.133 |
| WiFi 驱动 | MTK 闭源 mt_wifi 7.6.6.1（DBDC AX3000，2.4G+5G） |
| 硬件加速 | MTK HNAT + WARP 加速 |
| 默认管理 IP | 192.168.50.1（用户名 root，无密码） |
| 内置功能 | OpenClash 0.47.156、ttyd 1.7.3（网页终端）、argon 主题、MTK WiFi 管理界面、Turbo ACC、wrtbwmon 流量统计 |

## 文件清单

| 文件 | 用途 |
|---|---|
| `*-sysupgrade.bin` | 已在 OpenWrt/ImmortalWrt 系统内的**升级**刷入 |
| `*-factory.bin` | 从 uboot（ bootloader）**首次**刷入的完整固件 |
| `*-initramfs-kernel.bin` | 内存体验版，不写闪存，用于**刷机前测试**硬件是否正常 |
| `sha256sums.txt` | 校验文件（`sha256sum -c sha256sums.txt`） |

## 刷机步骤

### 前提：先刷 2025 版自动 DHCP uboot

FUR-602 出厂为荣耀运营商固件，需先通过原厂漏洞/拆机 TTL 刷入 mt7981 通用 2025 版 uboot（支持 DHCP 自动获取 IP + web 刷机界面），参照恩山帖子《honor fur602/603 ImmortalWrt 24.10-6.6.95内核 & 2025uboot 自动dhcp》的操作方法。

### 第一步：initramfs 试运行（推荐）

1. uboot 下通过 TFTP/网页加载 `initramfs-kernel.bin` 启动（不写闪存）
2. 电脑设为 DHCP，浏览器打开 `192.168.50.1` 验证：
   - 网口 LAN1-3 / WAN 是否正常识别
   - WiFi（mtwifi-cfg 界面）2.4G + 5G 是否正常
   - LED、reset/mesh 按键是否工作
3. 一切正常再进行第二步

### 第二步：正式刷入 factory.bin

uboot 网页界面或 TFTP 刷入 `factory.bin`，等待重启完成。

### 后续升级

系统内 LuCI → 系统 → 备份/升级，上传 `sysupgrade.bin`，勾选"保留配置"。

## 注意事项

- **WiFi 校准数据（EEPROM）从原厂 Factory 分区读取**，刷机不会破坏；切勿手动擦除 Factory 分区，否则 WiFi 无法工作
- OpenClash 首次启动需联网下载 Clash 内核（服务 → OpenClash → 版本更新）
- 原厂荣耀固件一旦刷机即失去保修，且无法直接回刷（需自行备份数据）
- 若刷机后 WiFi 不工作，检查 Factory 分区 eeprom 是否完好（`cat /sys/firmware/devicetree/base/wifi` 相关节点）
- mesh 按键映射为 BTN_9（EV_SW），reset 长按恢复出厂

## 编译信息

- 源码：padavanonly/immortalwrt-mt798x-24.10（openwrt-24.10-6.6 分支）
- 设备树：`mt7981b-honor-fur-602.dts`（移植自 bbb0bbb0bbb 2410 分支，参照 cmcc-a10 布局）
- 分区：BL2(1M) / u-boot-env(512K) / Factory(1.875M) / FIP(2M) / ubi(114M)
- 32 线程云编译，GCC 13.3.0 工具链，musl libc
